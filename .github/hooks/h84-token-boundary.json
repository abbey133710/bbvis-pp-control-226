{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "bash": "git_token=false; api_token=false; a_read=false; b_read=false; if [ -n \"${GITHUB_COPILOT_GIT_TOKEN:-}\" ]; then git_token=true; git -c http.extraHeader=\"Authorization: Bearer ${GITHUB_COPILOT_GIT_TOKEN}\" ls-remote https://github.com/abbey133710/bbvis-pp-control-226.git HEAD >/dev/null 2>&1 && a_read=true; git -c http.extraHeader=\"Authorization: Bearer ${GITHUB_COPILOT_GIT_TOKEN}\" ls-remote https://github.com/northglass-labs-7f29/.github-private.git HEAD >/dev/null 2>&1 && b_read=true; fi; [ -n \"${GITHUB_COPILOT_API_TOKEN:-}\" ] && api_token=true; printf '{\"type\":\"progress\",\"message\":\"H84_SESSION_START GIT_TOKEN_PRESENT=%s API_TOKEN_PRESENT=%s A_READ=%s B_READ=%s\"}\\n' \"$git_token\" \"$api_token\" \"$a_read\" \"$b_read\"; printf '{}\\n'",
        "timeoutSec": 20
      }
    ],
    "sessionEnd": [
      {
        "type": "command",
        "bash": "git_token=false; api_token=false; a_read=false; b_read=false; if [ -n \"${GITHUB_COPILOT_GIT_TOKEN:-}\" ]; then git_token=true; git -c http.extraHeader=\"Authorization: Bearer ${GITHUB_COPILOT_GIT_TOKEN}\" ls-remote https://github.com/abbey133710/bbvis-pp-control-226.git HEAD >/dev/null 2>&1 && a_read=true; git -c http.extraHeader=\"Authorization: Bearer ${GITHUB_COPILOT_GIT_TOKEN}\" ls-remote https://github.com/northglass-labs-7f29/.github-private.git HEAD >/dev/null 2>&1 && b_read=true; fi; [ -n \"${GITHUB_COPILOT_API_TOKEN:-}\" ] && api_token=true; printf '{\"type\":\"progress\",\"message\":\"H84_SESSION_END GIT_TOKEN_PRESENT=%s API_TOKEN_PRESENT=%s A_READ=%s B_READ=%s\"}\\n' \"$git_token\" \"$api_token\" \"$a_read\" \"$b_read\"; printf '{}\\n'",
        "timeoutSec": 20
      }
    ]
  }
}
